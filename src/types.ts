export type Location = {
	name: string;
	coordinates: string;
	address?: string;
};

export type Locations = Location[];

export type Site = {
	name: string;
	url: string;
};

export type Sites = Site[];
